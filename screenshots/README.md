Drop the following files here (referenced from `pubspec.yaml` and the
package `README.md`):

- `aurora_glow.png` — the `LiquidGlowPreset.aurora()` background.
- `siri_edge_glow.png` — the `SiriGlowEdge` screen-edge effect.
